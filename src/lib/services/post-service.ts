// Phase 2: post-service. Stubbed in Phase 1 — implementation lands in Phase 2.
//
// Will own weekly batch creation, per-post status transitions
// (draft → accepted/edited/skipped), and the regeneration counter used by
// the trial-limit gate.

export const postService = {} as const;
