// Phase 5: account-service. Stubbed in Phase 1 — implementation lands in Phase 5.
//
// Will own OAuth connect/disconnect for Facebook, Instagram, and LinkedIn,
// AES-256-GCM token encryption (via ENCRYPTION_KEY env), and token-refresh.

export const accountService = {} as const;
